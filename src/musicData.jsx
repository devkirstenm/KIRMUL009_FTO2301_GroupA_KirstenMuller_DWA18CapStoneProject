// you might not need this file because we get data from API
// musicData is an object, with a data property, which has a music property (access by musicData.data.musicShows)
export default {
  "success": true,
  "data": {
    "musicShows": [
      
    ]
  }
}
